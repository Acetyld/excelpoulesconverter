export default defineAppConfig({
  ui: {
    colors: {
      primary: 'orange',
      neutral: 'slate'
    },
    card: {
      slots: {
        root: 'bg-slate-800/50 ring-1 ring-slate-700 divide-y divide-slate-700'
      }
    }
  }
}) 